What happens when you initialize a repository? Why do you need to do it?
when we initialize a repository , a new folder called .git gets created and we are doing it to have multiple versions of the the directory